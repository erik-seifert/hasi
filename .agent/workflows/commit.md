---
description: Prepare a commit and a PR
---

- always use the /analyze workflow
- use github cli
- if the current local branch is the develop branch create a new branch
- check if the current branch is already merged
- if the branch is already merge create a new branch from the develop
- Commit the changes with a message
- Create a pull request with a title and a message which contains QA steps